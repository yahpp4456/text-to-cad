# 複製為 .env.local(已被 .gitignore 忽略)後填入。
#
# ── 認證(二選一;兩者都設時採 API key)──
#
# A. 自用(綁訂閱;需 Claude Pro/Max/Team/Enterprise):
#    終端機跑一次 `claude setup-token`,把印出的一年期 token 貼到這裡。
#    ⚠ 訂閱 OAuth 僅限訂閱者本人使用;把它拿去服務第三方使用者違反 Anthropic 條款。
CLAUDE_CODE_OAUTH_TOKEN=

# B. 產品/多人(API 按量計費,Commercial Terms):
#    在 Claude Console(platform.claude.com)建 API key 貼到這裡。
# ANTHROPIC_API_KEY=

# 連接埠(預設 8788)
# CADCHAT_PORT=8788

# 啟動時 session GC:刪掉 models/.cadchat/ 下超過 N 天沒動過的 session 目錄
# (中斷殘留的半成品在期限內保留,供 rehydrate/修復)。預設 7;設 0 停用。
# CADCHAT_GC_DAYS=7

# agent 用的模型(不設 → 用你 Claude Code CLI 的全域預設,會跟著 /model 變)。
# 可用別名 sonnet / opus / haiku,或完整 ID:
#   claude-sonnet-5            平衡,建議日常用(訂閱額度消耗適中)
#   claude-opus-4-8            最強,複雜組合件;燒訂閱額度較快
#   claude-haiku-4-5-20251001  最快最省;簡單零件夠用,複雜幾何易失手
# CADCHAT_MODEL=claude-sonnet-5

# ── 打包散布(Electron 內測版;與本檔無關,列在這只為文件化)──
# build 機另建 .env.bake(gitignored)貼入要烙進安裝檔的 key 與預設:
#   ANTHROPIC_API_KEY=sk-ant-xxxx   # 必填(建議專屬 key + Console 花費上限)
#   CADCHAT_MODEL=claude-sonnet-5   # 選填(任何 CADCHAT_* 預設皆可)
#   CADCHAT_EFFORT=high
# 跑 `npm run bake-auth` 產 .env.baked(隨包散布;dev 機已有認證時不生效)。
